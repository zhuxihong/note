### 常用CSS样式设置/布局方法：

```css

设置过度：transition
更改指针：cursor：pointer （类似可点击状态）
样式：list-style：none 清除样式


文本相关：
文本颜色：color：
设置文本对齐：text-align: center;
设置文本装饰线：text-decoration:
underline 下划线
overline 上划线
line-through 删除线
none 取消装饰线
设置行高：line-height：

背景相关：

设置背景图 background-image: url（）;
设置背景图分布：backgrou-repea：  no-repeat；不重复 
设置背景图片偏移：background-position: Xpx Ypx;
设置背景图片大小：background-size：
连写：background：url（） 重复方式 偏移量 ；
设置半透明：rgba


字体相关:
设置字体大小：font-size
设置字体加粗：font-weight:bold
设置字体样式：font-style: 参数:italic倾斜 normal不倾斜
设置字体：font-famil：  参数：字体名称
连写：font：是否倾斜；是否加粗；字体大小；字体名称；
设置字体单位：1rem（相对于根元素） 1em（相对于父元素） 相对于父/根元素的字体的倍率；

选择器：
id选择器：#id{}
类选择器：.class{}
群组选择器：选择器，选择器，{}
后代选择器：选择器 选择器{}   可以透过多层
子代选择器：选择器>选择器{}	  只能透过一层

伪类选择器：
：link    超链接访问前状态
：visited	超链接访问后的状态
：hover    鼠标划过时的状态
：active   鼠标按下时的状态
：focus    焦点状态

选择器权重:
标签选择器：1
伪类选择器：10
id选择器：100
行内样式：1000

布局相关：
更改元素显示方式：display   
设置参数： block块元素  inline-block 行内块素  inline 行内元素  none隐藏


盒子模型：
overflow 
设置内容尺寸  visible默认值，溢出显示  hidden溢出部分隐藏， s  auto自动
设置外边距margin
设置内边距padding
边框：border：边框大小 样式 颜色   样式：solid 实线 dotted点线 dashed虚线边框 double双线
指定方向：border-top ···
圆角边框：border-radius  取值：4个值上右下左 2个值：上下 左右 

布局方式：
浮动定位布局：
float:  取值（上下左右）
元素设置浮动后会脱离文档流（漂浮起来，图层上层），完全脱离原有布局方案，如行内元素，不用设置块属性。就可以设置宽高等属性，在文档中不占位置。
解决方案：
1.在父元素的末尾写一个元素，添加clear：both属性。清除浮动，
2.为父元素增加overflow：hidden

定位布局：
position： 取值：relative（相对定位）/absolute（绝对定位）/fixed（固定定位）
相对定位（以元素在页面中的原始位置为参照物）
绝对定位（参照离他最近的已经定位的祖先元素进行偏移，如果没有则参照窗口，脱离文档流，子绝父相。）
固定定位（参照窗口定位，不跟随网页滚动而滚动）
操作：上下左右的值，
定位特点：不脱离默认布局，会在原始位置保留元素的位置。适合对页面元素位置微调。绝对定位脱离文档流，不在默认布局中占据位置。通常在大范围元素内调整小元素的样式时使用。
图层：z-index:数字 数值越大越考上

```

### JavaScript:

```javascript
※ 变量声明：

JavaScript执行前会检查当前作用域所有声明的变量和函数，声明提前，值在顺序执行时再依次赋值。

- var全局变量 
- let局部变量 
- const 常量声明
----------------------------------------------------
※ 数据类型：

1. number 数值类型 
2. string  字符串类型   格式化字符串`hello ${变量名}`
3. boolean 布尔类型
4. undefined 为赋值（系统返回）
5. null 空（主动使用）
----------------------------------------------------

※ 数据类型转换：
转换字符串：数据.tosrting   
转换数字类型：Number（数据）
转换数字类型：parseInt（数据） 解析到无法解析的内容结束，返回解析到的内容.用于可取整
解析浮点数：parsefloat（数据） 能识别第一次出现的浮点数
----------------------------------------------------

※ 隐性转换：
字符串加其他类型，会把其他类型转换成字符串拼接；
字符串减数字类型，会把字符串转换成数字类型，做减法；
可以运用这个特性，去方便的转换数据类型；如果 123+""='123'   '123'-0=123
----------------------------------------------------

※ 【运算符】
++ -- 先返回最开始的数，再自加1
< > ==  比较符，会把值转换成数值笔记。特殊的：null==undfinded
=== 全等，类型和数值都一直
----------------------------------------------------

※ 逻辑运算符：
&&与  前面满足后面继续运行，前面不满足后面则不需要执行
||或  前不为真会继续执行后面的，有一处为真则为真
! 非  
----------------------------------------------------

※【三目运算】：
表达式1 ?表达式2:表达3
判断表示1是否成立，返回布尔值，如果表达式1满足执行表达式2，不满足执行表达式3。类似python，if..eles...
----------------------------------------------------
※【分支语句】：

if(条件表达式){
    表达式成立执行的代码段
}else if{
    表达式成立执行的代码段
}

switch(){
        case 值1：
            执行代码块
            break;
        case 值2：
            执行代码块
            break;
        case 值1：
            执行代码块
            break;
};
ps： switch 只能匹配单个值，可不写break跳出语句，会执行多个值
例：    
switch(){
        case 值1：
        case 值2：
            执行代码块
            break;
        case 值1：
            执行代码块
            break;
    	default:
    		未匹配到值执行
};
----------------------------------------------------

【循环语句】
while(循环条件){
    条件满足时执行代码段
}
do{
    无论如何都会执行一次
}while(){}
for循环：
for(定义循环变量；循环条件；更新循环变量){};
        
----------------------------------------------------

定义函数：
function 函数名（）{函数体};
声明提前，函数名提前时会携带函数体代码。可以在任意位置调用函数不分前后。
...values 可传递多个参数；

匿名函数：
!function(){};
定义：(function(){});
赋值：var fun=function(){};
自调用：(function(){})();
如果变量名和函数名相同时，声明提前函数会比函数优先级比较高。函数名和变量名不能重复，变量会覆盖同名的函数；函数内部定义的函数或变量，会跟随函数结束而销毁；如果需要使用函数内部的变量或函数，可以使用返回；
----------------------------------------------------
        
事件绑定：
 var btn=document.getElementById('bn');
 btn.onclick=function(){};
        
----------------------------------------------------
作用域：
声明提前会提起到当前作用域上方，作用域内没有声明，会修改外部作用域。通过传参不会修改外部作用域。修改的是参数；如果在函数内修改全局变量，直接使用不加声明即可，若果在函数内使用全局变量的值，不修改全局变量，将全局变量作为参数传递给函数使用。

----------------------------------------------------
DOM节点操作：
获取元素节点：
let bnt=document.getElementById('bn')
css选择器：
document.querySelector('')
        
获取元素属性/值：(标签包裹的内容)
元素.innerHTML   能获取到元素包裹的全部标签和内容
元素.innerText   获取标签包裹的全部文本内容

元素.getAttribute(属性)  获取指定属性名的值
元素.setAttribute(属性，值)  添加指定属性名和值
元素.removeAttribute(属性)  移除属性

更改样式：
元素.style="样式"
----------------------------------------------------
定时器：单位毫秒（1000=1秒）
setInterval(函数，时间间隔)； 周期性定时器
setTimeout(函数，时间间隔)； 一次性定时器
停止定时器：
clearInterval 
clearimeout	   
ps：停止定时器需传递一个ID值；创建定时器会返回一个id值；

各种事件现用现查！
```

### jQuery:

```javascript
引入：
<script src="https://code.jquery.com/jquery-1.11.3.min.js"></script>
选择器：$('css选择器')

相邻兄弟选择器（查找紧挨着的第一个兄弟元素）：$('.first+li')
通用兄弟选择器（后面所有的兄弟）：$('.first~li')

过滤器：
:first 第一个
:last 最后一个
:odd 奇数下标
:even 偶数下标
:eq(索引值) 匹配制定索引值
...更多根据需求查找
：not（）条件以外的其他元素
$('li:first')

样式相关
jq对象.css(属性，值) 只写一个属性值获取。

设置标签内容/值：
html（）
text（）
val（）  value

操作属性：
读取/写入：attr（属性名，值）
         prop（属性名，值）表单元素的选中状态必须用这个
移除：removeattr（属性名）

clss相关：
addclass 添加clss名
removclass 移除class名
togleclass 更新class名

this返回的是js对象，$(this)转换成jq对象；
链式操作，jq执行完函数后会返回对象。

页面元素添加与删除：
$('标签').attr('属性'，'值').css()  或直接在标签内写好样式和属性
查找页面中的元素插入到前面或后面。
末尾添加：$(body).append（制作的元素）
顶部添加：$(body).prepend(制作的元素)
元素前面添加：$(body).before()
元素后面添加：$(body).after()
移除：$(body).remove()
生成一次使用一次。append可以直接添加字符串;

动画效果：
jq提供了动画效果
显示/隐藏 hide（过度速度，执行结束后执行的函数）  show()

遍历：
each（function（下标，值）{
    consolelog(下标，值)
}）

调用全局方法：
$.each(对象，function（下标，值）{
    consolelog(下标，值)
})
PS:进来减少在页面中添加/移除元素操作。可以考虑等待所有元素拼接在一起，一次性的添加到页面

事件处理：

```

